apply plugin: eclipsebuild.BundlePlugin

configurations { lib }

dependencies { lib 'org.gradle:gradle-script-kotlin-tooling-models:+' }

task updateKotlinModelJar(type: Copy) {
    into '.'
    from configurations.lib
}